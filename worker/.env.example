# MongoDB connection string
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/sentiment

# Twitter/X API (optional - Reddit is used as fallback)
TWITTER_BEARER_TOKEN=your_bearer_token

# Hugging Face (optional - only needed for private models)
HUGGINGFACE_API_KEY=hf_your_api_key

# Backend API URL (for saving results)
API_BASE_URL=http://localhost:8000

# Default search settings
DEFAULT_KEYWORD=AI
DEFAULT_LIMIT=100
