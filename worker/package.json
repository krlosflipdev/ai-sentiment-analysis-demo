{
  "name": "@sentiment/worker",
  "private": true,
  "version": "0.1.0",
  "description": "Sentiment analysis worker using Hugging Face Transformers",
  "scripts": {
    "dev": "cd .. && ./worker/venv/bin/python -m worker.main --dry-run",
    "start": "cd .. && ./worker/venv/bin/python -m worker.main"
  }
}
