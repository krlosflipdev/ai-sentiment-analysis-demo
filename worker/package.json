{
  "name": "@sentiment/worker",
  "private": true,
  "version": "0.1.0",
  "description": "Sentiment analysis worker using Hugging Face Transformers",
  "scripts": {
    "dev": "python3 -u -m worker.main --dry-run",
    "start": "python3 -m worker.main"
  }
}
